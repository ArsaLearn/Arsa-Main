/*
  برنامه چشمک زن led                                     
          blink(زمان خاموش بودن,زمان روشن بودن,تعداد روشن و خاموش شدن,اسم پایه);  
    تابع تاخیر که سبب می شمو به میزان مشخص شده برنامه صبر کند                  
                                 delay(عدد بر حسب میلی ثانیه);
 */
#include <arsalearn.h>
void setup() {
  run();
}
void loop() {
blink(led,3,100,100);
delay(1000);
blink(led,3,1000,1000);
delay(1000);
}
