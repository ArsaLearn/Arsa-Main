/*
  برنامه چشمک زن led                                     
                                      blink(اسم پایه);  
    تابع تاخیر که سبب می شمو به میزان مشخص شده برنامه صبر کند                  
                                 delay(عدد بر حسب میلی ثانیه);
 */
#include <arsalearn.h>
void setup() {
  run();
}
void loop() {
blink(led);
}
